<common:PageBase
    x:Class="TouchPointWindows.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TouchPointWindows.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:common="using:TouchPointWindows.Common"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    	<StackPanel Margin="0" Orientation="Vertical" VerticalAlignment="Center">
    		<Button Content="Add Items" Command="{Binding AddMenuItemCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="200"/>
    		<Button Content="Order" Command="{Binding TakeOrderCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="200"/>
    	</StackPanel>
    </Grid>
</common:PageBase>
